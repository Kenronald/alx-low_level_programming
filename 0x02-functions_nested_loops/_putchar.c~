#include "main.h"
#include <unistd.h>
/**
*_putchar - writes a programe that prints A putchar file
*"c: The character to print
*
*Return: On success 1.
ON error, -1 is returned and error is set
*/
int _putchar(char c)
{
return (write(1, &c, 1));
}
